import { Link } from "@tanstack/react-router";
import { Menu, Moon, Sun, X } from "lucide-react";
import { useState, type ReactNode } from "react";
import { Button } from "@/components/ui/button";
import { useTheme } from "@/hooks/useTheme";
import { DISCLAIMER } from "@/services/documents/documents";
const NAV=[{to:"/",label:"Home"},{to:"/ask",label:"Ask Dami"},{to:"/research",label:"Saved research"},{to:"/sources",label:"Sources"},{to:"/documents",label:"Documents"},{to:"/settings",label:"Settings"}] as const;
export function AppShell({children}:{children:ReactNode}){const{toggle}=useTheme();const[open,setOpen]=useState(false);return <div className="flex min-h-screen flex-col bg-background text-foreground"><header className="sticky top-0 z-30 border-b border-border/60 bg-background/90 backdrop-blur"><div className="mx-auto flex max-w-6xl items-center justify-between gap-2 px-4 py-3"><Link to="/" className="whitespace-nowrap text-lg font-semibold">Dami<span className="text-primary"> AI</span></Link><nav className="hidden items-center justify-center gap-1 text-sm md:flex">{NAV.map(item=><Link key={item.to} to={item.to} className="rounded-md px-3 py-1.5 text-muted-foreground hover:bg-accent hover:text-foreground [&.active]:bg-accent [&.active]:text-foreground">{item.label}</Link>)}</nav><div className="flex items-center gap-1"><Button variant="ghost" size="icon" onClick={toggle} aria-label="Switch theme"><Sun className="h-4 w-4 dark:hidden"/><Moon className="hidden h-4 w-4 dark:block"/></Button><Button variant="ghost" size="icon" className="md:hidden" onClick={()=>setOpen(v=>!v)} aria-label="Open navigation">{open?<X className="h-5 w-5"/>:<Menu className="h-5 w-5"/>}</Button></div></div>{open&&<nav className="grid gap-1 border-t border-border/60 px-4 py-3 md:hidden">{NAV.map(item=><Link key={item.to} to={item.to} onClick={()=>setOpen(false)} className="rounded-lg px-3 py-2.5 text-sm text-muted-foreground hover:bg-accent hover:text-foreground [&.active]:bg-accent [&.active]:text-foreground">{item.label}</Link>)}</nav>}</header><main className="mx-auto w-full max-w-6xl flex-1 px-4 py-5 sm:py-10">{children}</main><footer className="border-t border-border/60 py-5"><div className="mx-auto max-w-3xl space-y-3 px-4 text-center text-xs leading-relaxed text-muted-foreground"><p>{DISCLAIMER}</p><div className="flex items-center justify-center gap-4"><Link to="/privacy">Privacy Policy</Link><Link to="/terms">Terms of Use</Link></div></div></footer></div>}
